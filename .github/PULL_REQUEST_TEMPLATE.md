## Description of Changes
Summarize the changes made in this pull request.

## Checklist
- [ ] Code follows POSIX/Bash strict mode (`set -euo pipefail`).
- [ ] Syntax audit passed (`bash -n` and `zsh -n`).
- [ ] No core identity elements altered (Powerlevel10k, IrBlack, JetBrains Mono font, Ctrl+O, Ctrl+E).
